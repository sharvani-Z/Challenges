import type { Meta, StoryObj } from "@storybook/react";
import ChallengeList from "./index";

const meta: Meta<typeof ChallengeList> = {
  title: "Organisms/ChallengeList",
  component: ChallengeList,
};

export default meta;

export const Default: StoryObj<typeof ChallengeList> = {};
